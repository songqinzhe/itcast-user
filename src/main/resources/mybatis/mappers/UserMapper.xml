<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.itcast.user.mapper.UserMapper" >
<!--    <select id="queryUserById" parameterType="java.lang.Long" resultType="cn.itcast.user.pojo.User">-->
<!--        select * from tb_user where id = #{id}-->
<!--    </select>-->

    <delete id="delete1" parameterType="java.lang.Long">
        delete from tb_user where id = #{id}
    </delete>

<!--    <insert id="insert" parameterType="cn.itcast.user.pojo.User">-->
<!--        insert into tb_user (id , username , password , name , age , sex , birthday , created ,updated) values ( #{id} , #{username} , #{password} , #{name} , #{age} , #{sex} , '2020-06-26' , #{created} ,#{updated})-->
<!--    </insert>-->

<!--    <update id="update" parameterType="cn.itcast.user.pojo.User">-->
<!--        update tb_user set  username = #{username} , password = #{password} , name = #{name} ,age= #{age} ,sex= #{sex}  where id = #{id}-->
<!--    </update>-->

</mapper>